---
# Debian server setup Playbook

    - name: update all packages on Debian family
      apt:
        name: "*"
        state: latest
        update_cache: yes

    - name: add the must have apps on Debian
      apt:
        name: "{{ item }}"
        state: latest
      with_items: "{{ must_have_apps }}"
